<Window x:Class="sZIP.App.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Text.ArchivePassword}" SizeToContent="Height" Width="440"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" Icon="Assets/szip.ico">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource Text.EnterArchivePassword}"
                   Margin="0,0,0,12" TextWrapping="Wrap" />
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Height="36"
                     VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="{DynamicResource Text.OK}" IsDefault="True" Width="76" Height="36"
                    Click="ConfirmButton_Click" />
            <Button Content="{DynamicResource Text.Cancel}" IsCancel="True" Width="76" Height="36"
                    Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
