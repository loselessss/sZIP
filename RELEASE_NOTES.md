# sZIP 1.4.0 릴리스 노트

릴리스 날짜: 2026-08-15

## 핵심 변경

- 메인 창과 암호·업데이트 대화상자에 Fluent 계열 공통 스타일을 적용했습니다.
- Windows의 밝은/어두운 앱 테마를 실행 중에도 반영합니다.
- 진행 화면에서 현재 항목, 퍼센트, 처리 용량, 평균 속도와 예상 남은 시간을 확인할 수 있습니다.
- `그냥 풀기`와 `알아서 풀기`를 분리했습니다.
- 압축 목록에서 여러 파일·폴더를 선택해 필요한 내용만 푸는 `선택 풀기`를 추가했습니다. 폴더를 선택하면 모든 하위 항목이 함께 풀립니다.
- Paper Organizer 방식의 GitHub Releases 업데이트 확인·릴리스 노트·버전 건너뛰기·SHA-256 검증 설치 흐름을 포함합니다.
- 트레이 메뉴뿐 아니라 메인 창의 `업데이트 확인` 버튼으로도 수동 확인할 수 있습니다.
- Windows 11의 `추가 옵션 표시`를 열지 않아도 기본 우클릭 메뉴에서 sZIP 명령을 사용할 수 있도록 네이티브 `IExplorerCommand`와 sparse identity package를 추가했습니다.
- Windows 10 및 modern 메뉴 등록 실패 환경에서는 기존 레거시 탐색기 메뉴를 유지합니다.

## 압축 해제 방식

- `그냥 풀기`: 지정한 폴더나 압축 파일이 있는 폴더에 내용물을 바로 놓습니다. 같은 이름이 있으면 덮어쓰지 않고 `(1)` 이름을 사용합니다.
- `알아서 풀기`: 압축 안에 최상위 폴더 하나만 있으면 그 폴더를 그대로 놓고, 여러 파일·폴더가 섞여 있으면 압축파일명 폴더를 만들어 정리합니다.
- `선택 풀기`: 목록에서 선택한 파일과 폴더만 지정한 위치에 원래 하위 경로를 유지해 풉니다. `Ctrl` 또는 `Shift`로 여러 항목을 선택할 수 있습니다.
- 이전 버전의 `--extract` 명령은 호환성을 위해 알아서 풀기로 처리합니다.

## 자동 검증

- .NET Framework 4.8 Release 빌드: 경고 0개, 오류 0개
- 자동 테스트: 39개 통과, 실패 0개
- 태그 워크플로에서 .NET 앱, x64 네이티브 셸 확장, sparse MSIX, Inno Setup 설치본을 순서대로 빌드합니다.

## 수동 QA

Windows 11 기본 우클릭 메뉴는 설치본 생성 후 Explorer 재시작 또는 다시 로그인한 환경에서 확인합니다. 사용자 요청에 따라 전체 수동 UI QA는 별도 단계에서 수행합니다.
